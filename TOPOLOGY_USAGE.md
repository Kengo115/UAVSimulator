# 外部トポロジファイル機能の使用ガイド

## 変更内容の概要

ネットワークトポロジ設定を外部ファイルから読み込めるようになりました。
これにより、ソースコードを変更せずに異なるネットワーク構成をテストできます。

## 新規作成されたファイル

1. **src/server/network/TopologyFileReader.java**
   - トポロジファイルを読み込むクラス
   - ノード情報とリンク情報をパース
   - バリデーション機能を含む

2. **topology/default_topology.txt**
   - 現在のデフォルト設定と同等のサンプルファイル
   - コメント付きで説明

3. **topology/README.md**
   - トポロジファイルの詳細なドキュメント

## 修正されたファイル

1. **src/item/BeaconCluster.java**
   - 外部ファイルから読み込んだノード情報から初期化できるコンストラクタを追加

2. **src/server/network/NetworkTopologyManager.java**
   - 外部ファイルから読み込んだリンク情報を設定できるメソッドを追加
   - 既存のハードコード設定も維持

3. **src/server/controller/ServerController.java**
   - トポロジデータを受け取るコンストラクタを追加

4. **src/controller/BoundaryController.java**
   - CUIでトポロジファイルのパスを入力できるように変更
   - デフォルト設定も選択可能

## 使用方法

### 基本的な使い方

1. プログラムを起動します：
```bash
java controller.BoundaryController
```

2. トポロジファイルのパス入力プロンプトが表示されます：
```
=== UAVシミュレーター ===
ネットワークトポロジファイルのパスを入力してください:
（Enterキーのみを押すとデフォルトトポロジを使用します）
>
```

3. 選択肢：
   - トポロジファイルのパスを入力（例: `topology/default_topology.txt`）
   - Enterキーのみ押してデフォルト設定を使用

### トポロジファイルを指定する場合

```
> topology/default_topology.txt
```

プログラムは以下を実行します：
1. ファイルを読み込む
2. ネットワーク情報を表示する
3. バリデーションを行う
4. シミュレーションを開始する

出力例：
```
トポロジファイルを読み込んでいます: topology/default_topology.txt
=== ネットワークトポロジ情報 ===
ノード数: 6

--- ノード一覧 ---
  Node 0: (0.10, 0.40)
  Node 1: (0.10, 0.70)
  Node 2: (0.50, 0.10)
  Node 3: (0.50, 0.40)
  Node 4: (0.50, 0.70)
  Node 5: (0.90, 0.40)

--- リンク一覧 ---
  Link 0↔1: 容量=5, 距離=250m, 長さ=1.0
  Link 0↔2: 容量=15, 距離=750m, 長さ=3.0
  ...
===============================

経路探索手法を選択してください:
...
```

### デフォルト設定を使用する場合

```
> [Enter]
```

従来通りハードコードされた設定でシミュレーションが実行されます。

## カスタムトポロジの作成

### ステップ1: ファイルを作成

`topology/`ディレクトリに新しい`.txt`ファイルを作成します：

```bash
touch topology/my_custom_topology.txt
```

### ステップ2: ノードを定義

```
# My Custom Network
NODES 4

NODE 0 0.2 0.2
NODE 1 0.2 0.8
NODE 2 0.8 0.2
NODE 3 0.8 0.8
```

### ステップ3: リンクを定義

```
LINK 0 1 10 300 1.5
LINK 0 2 10 300 1.5
LINK 1 3 10 300 1.5
LINK 2 3 10 300 1.5
```

### ステップ4: シミュレーションで使用

```
> topology/my_custom_topology.txt
```

## パラメータの意味と調整方法

### ノード座標 (X, Y)
- **範囲**: 0.0～1.0（正規化座標）
- **用途**: ネットワーク図の表示位置
- **推奨**: ノードが重ならないように配置

### リンク容量
- **意味**: 同時に飛行できるUAV数
- **一般的な値**: 5～15
- **調整**:
  - 小さい値（5）: 混雑しやすい
  - 大きい値（15）: 並列飛行可能

### 距離（メートル）
- **意味**: 実際の物理距離
- **一般的な値**: 250～850m
- **調整**: UAVの飛行時間に影響

### チューブ長さ (L_tubeLength)
- **意味**: PhysarumSolverアルゴリズムの相対コスト
- **一般的な値**: 1.0～3.3
- **調整**:
  - 小さい値: 優先的に使用される
  - 大きい値: 使用頻度が低くなる
  - 通常は距離に比例させる

## 既存の動作との互換性

### デフォルトモード（変更なし）
Enterキーのみを押すことで、従来と全く同じ動作をします：
- ハードコードされた6ノードトポロジ
- 既存のリンク設定
- すべての既存機能が正常に動作

### 外部ファイルモード（新機能）
トポロジファイルを指定した場合も、以下は変わりません：
- 経路探索アルゴリズム（Dijkstra, EPS, etc.）
- UAV飛行制御
- 結果出力
- ログ記録

## トラブルシューティング

### エラー: ファイルが見つかりません
```
java.io.FileNotFoundException: topology/my_file.txt
```
**解決策**:
- ファイルパスが正しいか確認
- プロジェクトルートからの相対パスを使用
- または絶対パスを使用

### エラー: ノード数が一致しません
```
IOException: ノード数が一致しません。宣言: 6, 定義: 5
```
**解決策**:
- NODESで宣言した数とNODE行の数を一致させる

### エラー: 座標が範囲外です
```
IOException: 座標は0.0～1.0の範囲である必要があります
```
**解決策**:
- すべての座標を0.0～1.0の範囲にする

### エラー: リンクが範囲外のノードを参照
```
IOException: リンクのsource IDが範囲外です: 10
```
**解決策**:
- リンクのノードIDが定義済みのノードを参照しているか確認

## 高度な使用例

### 大規模ネットワーク（20ノード）
```
NODES 20
NODE 0 0.1 0.1
NODE 1 0.1 0.3
...
NODE 19 0.9 0.9

# メッシュ構造のリンク定義
LINK 0 1 10 200 1.0
LINK 0 5 10 400 2.0
...
```

### スター型トポロジ（中心ハブ）
```
NODES 7
NODE 0 0.5 0.5  # 中心ハブ
NODE 1 0.2 0.2
NODE 2 0.2 0.8
NODE 3 0.8 0.2
NODE 4 0.8 0.8
NODE 5 0.5 0.1
NODE 6 0.5 0.9

# すべてのノードを中心に接続
LINK 0 1 15 500 2.0
LINK 0 2 15 500 2.0
LINK 0 3 15 500 2.0
LINK 0 4 15 500 2.0
LINK 0 5 15 500 2.0
LINK 0 6 15 500 2.0
```

### リング型トポロジ
```
NODES 6
NODE 0 0.5 0.1
NODE 1 0.9 0.3
NODE 2 0.9 0.7
NODE 3 0.5 0.9
NODE 4 0.1 0.7
NODE 5 0.1 0.3

# リング状に接続
LINK 0 1 10 300 1.5
LINK 1 2 10 300 1.5
LINK 2 3 10 300 1.5
LINK 3 4 10 300 1.5
LINK 4 5 10 300 1.5
LINK 5 0 10 300 1.5
```

## まとめ

この機能により：
- ✅ ソースコード変更不要でトポロジ変更可能
- ✅ 複数のトポロジを簡単にテスト可能
- ✅ 既存機能との完全互換性
- ✅ エラーハンドリングとバリデーション
- ✅ 明確なドキュメント

従来の方法（デフォルトトポロジ）も完全にサポートされており、
既存のワークフローは変更なく動作します。
